namespace CordelUTE;

public partial class AppShell : Shell
{
    // Default constructor for the AppShell class. This constructor
    // is called automatically when the AppShell component is created.
    public AppShell()
    {
        // Initialize the application components defined in the
        // XAML file (likely AppShell.xaml) associated with this class.
        InitializeComponent();
    }
}
